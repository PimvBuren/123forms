// Selecteer het inputveld
const inputField = document.querySelector("116039232"); // Vervang #jouwInputID door de echte ID of class van je inputveld

// Voeg een eventlistener toe
inputField.addEventListener("116039232", function () {
    // Vervang punten door komma's
    this.value = this.value.replace(/\./g, ",");
});